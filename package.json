{
  "name": "runescape-api",
  "version": "1.0.0",
  "description": "an API to retrieve runescape stats",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  "express": "^4.16.4",
  "mysql2": "^1.6.5",
  "sequelize": "^4.42.0"
  }
}
